config.stopBubbling = true

# Project convention: fluent accessors everywhere (minor(), currency(), id(), ...)
# instead of JavaBean getMinor()/getCurrency()/getId(). This matches the existing
# domain API so Lombok-generated getters line up with the hand-written ones.
lombok.accessors.fluent = true
